<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- PRUEBA: solo clase del form + notebook mínimo. Si la vista aparece en Vistas heredadas y ves la pestaña "Supplies", el fallo está en los otros xpaths o campos. -->
  <record id="view_production_lot_form_inherit_supplies" model="ir.ui.view">
    <field name="name">production.lot.form.supplies.inherit</field>
    <field name="model">stock.lot</field>
    <field name="inherit_id" ref="stock.view_production_lot_form"/>
    <field name="priority" eval="99"/>
    <field name="arch" type="xml">
      <xpath expr="//form" position="attributes">
        <attribute name="class" add="o_stock_lot_pastel_form" separator=" "/>
      </xpath>
      <xpath expr="//group[@name='description']" position="before">
        <notebook>
          <page string="Supplies" name="supplies_test_tab">
            <group string="Prueba"><div class="text-muted">Si ves esta pestaña, la herencia funciona.</div></group>
          </page>
        </notebook>
      </xpath>
    </field>
  </record>
</odoo>
