<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Accion de servidor para vincular todos los lotes masivamente -->
    <record id="action_mass_link_all_lots" model="ir.actions.server">
        <field name="name">Vincular Todos por Lote</field>
        <field name="model_id" ref="stock.model_stock_lot"/>
        <field name="binding_model_id" ref="stock.model_stock_lot"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = model.action_mass_link_all_lots()</field>
    </record>
</odoo>