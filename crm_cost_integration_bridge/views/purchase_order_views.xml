<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_purchase_order_form_integration_bridge" model="ir.ui.view">
        <field name="name">purchase.order.form.integration.bridge</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <!-- Eliminado: la selección de ganadora ahora se ejecuta automáticamente
                 cuando approved_by_crm pasa a True en purchase.order -->
            <data/>
        </field>
    </record>
</odoo>
