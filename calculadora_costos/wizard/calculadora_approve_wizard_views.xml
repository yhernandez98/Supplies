<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_calculadora_approve_wizard_form" model="ir.ui.view">
        <field name="name">calculadora.approve.wizard.form</field>
        <field name="model">calculadora.approve.wizard</field>
        <field name="arch" type="xml">
            <form string="Aprobar cotización y cargar a lista de precios">
                <group>
                    <field name="calculadora_id" invisible="1"/>
                    <field name="partner_id"/>
                    <field name="approved_plazo_meses" widget="radio" options="{'horizontal': true}"/>
                    <field name="approved_escenario_key" widget="radio"/>
                </group>
                <footer>
                    <button name="action_approve_and_load" string="Aprobar y cargar a lista de precios" type="object" class="btn-primary"/>
                    <button string="Cancelar" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
