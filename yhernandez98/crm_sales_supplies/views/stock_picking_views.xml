<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extender vista de stock.picking para mostrar información de leasing -->
    <!-- TEMPORALMENTE COMENTADO debido a conflictos con otros módulos -->
    <!-- La información de leasing está disponible en otras vistas (productos, inventario, contratos) -->
    <!--
    <record id="view_stock_picking_form_inherit_leasing" model="ir.ui.view">
        <field name="name">stock.picking.form.leasing.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='operations']//field[@name='move_ids_without_package']//list/field[@name='product_id']" position="after">
                <field name="is_leasing" 
                       string="Leasing" 
                       widget="boolean_toggle"
                       optional="show"/>
                <field name="leasing_brand_id" 
                       string="Marca Leasing" 
                       optional="hide"/>
            </xpath>
        </field>
    </record>
    -->

    <!-- Extender vista de lista de stock.picking -->
    <!-- Comentada porque stock.view_picking_tree puede no existir en Odoo 18 -->
    <!--
    <record id="view_stock_picking_tree_inherit_leasing" model="ir.ui.view">
        <field name="name">stock.picking.tree.leasing.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='scheduled_date']" position="after">
                <field name="has_leasing_products" 
                       string="Leasing" 
                       widget="boolean_toggle"
                       optional="show"/>
            </xpath>
        </field>
    </record>
    -->
</odoo>

