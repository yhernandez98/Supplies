<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vista del wizard para crear contrato de leasing -->
    <record id="view_leasing_contract_wizard_form" model="ir.ui.view">
        <field name="name">leasing.contract.wizard.form</field>
        <field name="model">leasing.contract.wizard</field>
        <field name="arch" type="xml">
            <form string="Crear Contrato de Leasing">
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <span>📄 Crear Nuevo Contrato de Leasing</span>
                        </h1>
                    </div>
                    <div class="alert alert-info" role="alert">
                        <p><strong>💡 Información:</strong></p>
                        <p>Complete los datos del contrato de leasing. Este contrato será asociado automáticamente a la orden de compra.</p>
                    </div>
                    <group>
                        <group>
                            <field name="purchase_order_id" readonly="1" string="Orden de Compra Relacionada"/>
                            <field name="brand_ids" 
                                   widget="many2many_tags" 
                                   options="{'no_create': True}"
                                   placeholder="Seleccione una o más marcas (ej: HP, Dell, Lenovo)..."
                                   string="Marcas *"
                                   required="1"/>
                            <field name="partner_id" 
                                   options="{'no_create': True}"
                                   string="Cliente *"
                                   required="1"/>
                        </group>
                        <group>
                            <field name="start_date" string="Fecha Inicio *" required="1"/>
                            <field name="end_date" string="Fecha Fin (Opcional)"/>
                            <field name="template_id" options="{'no_create': False}" 
                                   domain="[('active', '=', True)]"
                                   help="Seleccione una plantilla para el contrato (opcional)."/>
                        </group>
                    </group>
                    <group>
                        <field name="notes" 
                               placeholder="Escriba aquí notas adicionales sobre el contrato (opcional)..."
                               widget="text"/>
                    </group>
                    <div class="alert alert-warning" role="alert">
                        <p><strong>⚠️ Importante:</strong></p>
                        <ul>
                            <li>Puede seleccionar múltiples marcas para este contrato.</li>
                            <li>El contrato se creará en estado "Borrador" y podrá editarlo después si es necesario.</li>
                            <li>Una vez creado, el contrato quedará asociado a esta orden de compra.</li>
                        </ul>
                    </div>
                </sheet>
                <footer>
                    <button name="action_create_contract" 
                            string="✅ Crear Contrato" 
                            type="object" 
                            class="oe_highlight"
                            icon="fa-check"/>
                    <button string="Cancelar" 
                            class="btn-secondary" 
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Acción para abrir el wizard -->
    <record id="action_leasing_contract_wizard" model="ir.actions.act_window">
        <field name="name">Crear Contrato de Leasing</field>
        <field name="res_model">leasing.contract.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>

