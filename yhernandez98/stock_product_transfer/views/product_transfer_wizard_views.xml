<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Vista formulario del wizard -->
        <record id="view_product_transfer_wizard_form" model="ir.ui.view">
            <field name="name">product.transfer.wizard.form</field>
            <field name="model">product.transfer.wizard</field>
            <field name="arch" type="xml">
                <form string="Conversión Genérico → Específico">
                    <sheet>
                        <group>
                            <field name="operation_mode" invisible="1"/>
                            <group string="Información de Conversión">
                                <field name="location_id" 
                                       options="{'no_create': True, 'no_create_edit': True}"
                                       string="Ubicación"/>
                                <field name="source_product_id" 
                                       options="{'no_create': True, 'no_create_edit': True}"
                                       context="{'default_type': 'product'}"
                                       string="Producto Origen"/>
                                <field name="destination_product_id" 
                                       options="{'no_create': True, 'no_create_edit': True}"
                                       context="{'default_type': 'product'}"
                                       string="Producto Destino"/>
                                <field name="product_uom_id" readonly="1" string="Unidad de Medida"/>
                            </group>
                            <group string="Serial Genérico">
                                <field name="lot_ids" 
                                       domain="[('id', 'in', available_lot_ids)]" 
                                       widget="many2many_tags" 
                                       options="{'no_create': True, 'no_create_edit': True}" 
                                       placeholder="Seleccione el serial del producto genérico a convertir (solo uno)..."/>
                                <div class="alert alert-info" role="alert" invisible="not available_lot_ids">
                                    <p>Seriales disponibles en <strong><field name="location_id" readonly="1"/></strong>: <field name="available_lot_ids" readonly="1" widget="statinfo" string="Seriales"/></p>
                                    <p><small>Seleccione exactamente un serial del producto genérico.</small></p>
                                </div>
                            </group>
                        </group>
                        
                        <notebook>
                            <page string="Detalles de Conversión" name="conversion_details">
                                <group>
                                    <field name="operation_mode" invisible="1"/>
                                    
                                    <div class="alert alert-warning" role="alert">
                                        <p><strong>⚠️ Modo Conversión:</strong> El producto genérico y su serial serán eliminados completamente. Se creará una unidad del producto específico en la ubicación seleccionada.</p>
                                    </div>
                                </group>
                            </page>
                            
                            <page string="Notas" name="notes">
                                <field name="note" placeholder="Agregar nota sobre la conversión..."/>
                            </page>
                        </notebook>
                    </sheet>
                    <footer>
                        <button string="🔍 Debug Info" name="action_show_debug_info" type="object" class="btn-secondary" 
                                invisible="not lot_ids"/>
                        <button string="Convertir" name="action_transfer" type="object" class="btn-primary"/>
                        <button string="Cancelar" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <!-- Acción para abrir el wizard -->
        <record id="action_product_transfer_wizard" model="ir.actions.act_window">
            <field name="name">Conversión Genérico → Específico</field>
            <field name="res_model">product.transfer.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <field name="binding_model_id" ref="stock.model_stock_quant"/>
            <field name="binding_view_types">list</field>
        </record>
    </data>
</odoo>

